import { constant } from "../constants";

export enum EENV {
    DEV = constant.DEV,
    QA = constant.QA,
    PROD = constant.PROD
}

export enum EStatus {
    SUCCESS = constant.SUCCESS,
    FAILURE = constant.FAILURE
}
